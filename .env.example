# Streamlit Cloud Environment Variables Configuration
# Copy this file to .env and update the values for your deployment

# Backend API Configuration
# For Streamlit Cloud deployment, you need to set up a separate backend API service
# and update these values with the actual backend API URL
BACKEND_API_HOST=your-backend-api-url.com
BACKEND_API_PORT=8000
BACKEND_API_USERNAME=admin
BACKEND_API_PASSWORD=admin

# Authentication System
AUTH_SYSTEM_ENABLED=False

# Note: In Streamlit Cloud, you need to:
# 1. Deploy the Hummingbot Backend API separately
# 2. Update BACKEND_API_HOST with your backend API's public URL
# 3. Set up proper authentication if needed

# For local development with Docker:
# BACKEND_API_HOST=backend-api
# BACKEND_API_PORT=8000